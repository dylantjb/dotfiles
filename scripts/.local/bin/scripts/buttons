#!/bin/sh

device="Wacom Intuos S 2 Pad pad"

[ "$(xsetwacom list)" ] || exit 0

xsetwacom set "$device" Button 1 "key +ctrl z"
xsetwacom set "$device" Button 3 "key +ctrl y"
xsetwacom set "$device" Button 8 "key +ctrl minus"
xsetwacom set "$device" Button 9 "key +ctrl +shift ="
