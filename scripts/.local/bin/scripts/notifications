#!/usr/bin/env bash

blacklist=( "Spotify" "dunstify" "Brave" )
sound="$HOME/.config/dunst/sounds/$1.wav"

[[ ! " ${blacklist[@]} " =~ " $1 " ]] || [[ "$5" = "CRITICAL" ]] && {
    aplay "$sound" || aplay ~/.config/dunst/sounds/alert.wav
}

